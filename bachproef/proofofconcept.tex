\chapter{\IfLanguageName{dutch}{Proof of concept}{Proof of concept}}%
\label{ch:proof-of-concept}

Dit hoofdstuk beschrijft de ontwikkeling van de proof-of-concept (POC) voor het automatisch detecteren van de open of gesloten status van supermarkten op basis van historische POI-data. De POC is volledig geïmplementeerd in Python en bestaat uit de volgende fasen: data en bibliotheken inladen, data analyse, feature engineering, het trainen van de modellen en evaluatie. De implementatie is gedocumenteerd in een Jupyter Notebook. De volledige broncode van de POC is beschikbaar op GitHub: \url{https://github.com/MaximVDS/BP_Maxim_Van_Der_Schueren_2526}.

\section{Databronnen en datastructuur}
\subsection{Geografische scope}
Een eerste belangrijke beslissing is het beslissen van de geografische scope van de dataset. De interne POI-databank van Accurat bevat records voor meerdere Europese landen. Voor de ontwikkeling van deze POC werd gekozen om vier coherente landen te gebruiken: België, Nederland, Luxemburg en Frankrijk. Deze vormen een regio waarin vergelijkbare supermarktketens actief zijn, wat de vergelijkbaarheid van de geleerde patronen ten goede komt. 

\subsection{Basisinformatie van de data}
Voor deze POC werden twee afzonderlijke datasets gebruikt die door Accurat BV ter beschikking werden gesteld.

\paragraph{Historische dataset}
Deze dataset vormt de primaire bron voor het trainen van de drie modellen en bevat in totaal 1.337.279 POI-records voor België, Nederland, Luxemburg en Frankrijk. De dataset is opgebouwd uit drie gekoppelde tabellen: een hoofdtabel met basisinformatie per POI (naam, adres, geografische coördinaten, merk, bron en operationele status), een historische statustabel die onder andere de \texttt{is\_active} gegevens doorheen de tijd bijhoudt, en een tabel van de openingsuren met de dagelijkse openings- en sluitingstijden per POI.

\begin{listing}[H]
    \begin{minted}{python}
        df_raw = pd.read_csv(DATA_PATH, low_memory=False)
        print(f"Geladen: {df_raw.shape[0]:,} rijen x {df_raw.shape[1]} kolommen")
        df_raw.head(3)
    \end{minted}
    \caption[Data inladen]{Inladen van de historische dataset.}
\end{listing}

\paragraph{Basisinfo dataset}
%TODO: aantal records controleren
Deze dataset bevat enkel de meest recente operationele toestand van elke POI op het moment, zonder de historische statusregistraties. Deze dataset telt 120.422 records en 29 kolommen, en werd gebruikt als basis voor het uitbreidingsexperiment met kunstmatig gegenereerde mobiliteitsdata (zie sectie~\ref{sec:mobiliteitsdata}).

\begin{listing}[H]
    \begin{minted}{python}
        df_basis = pd.read_csv("../../data accurat/Basisinfo2.csv", low_memory=False, delimiter=";")
        
        label_counts = df_basis["is_active"].value_counts().sort_index()
        label_pct    = label_counts / len(df_basis) * 100
    \end{minted}
    \caption[Inladen Basisinfo-dataset]{Inladen van de momentopname-dataset gebruikt als basis voor het mobiliteitsexperiment.}
\end{listing}

\vspace{1em}

Beide datasets worden op regelmatige basis bijgewerkt: de data van Accurat wordt elke 30 dagen ververst. Dit betekent dat er steeds een validatieachterstand kan optreden van maximaal één maand tussen de werkelijke toestand van een POI en de toestand die in de databank is geregistreerd. Dit is een uitdaging die aansluit bij de bespreking van de data-actualiteit in sectie~\ref{sec:uitdagingen}.

% TODO: Nagaan of de vermelding van Locatus en andere externe bronnen (naast zelf gescrapte data) expliciet vermeld mag worden in de bachelorproef. De data van Accurat is afkomstig uit een combinatie van eigen scraping en externe dataleveranciers zoals Locatus.
De data is afkomstig uit meerdere bronnen: enerzijds vergaart Accurat zelf data van de POI's via HTML en API scraping, anderzijds worden ook data van externe dataleveranciers gebruikt. Dezen aanpak vergroot de dekking maar introduceert ook de uitdagingen van POI-conflation zoals besproken in sectie~\ref{sec:gegevensbronnen}.

\section{Data-analyse}

\subsection{Verhouding van actieve en inactieve records}

Een eerste stap in de data-analyse bestond uit het in kaart brengen van de verdeling tussen de twee klassen in de doelvariabele \texttt{is\_active}. De labelstrategie is als volgt gedefinieerd:

\begin{itemize}
    \item is\_active = 1 duidt op een open POI: de scraper was actief en registreerde de POI als operationeel.
    \item is\_active = 0 duidt op een gesloten POI: de scraper was actief maar registreerde de POI als inactief op dat moment.
\end{itemize}

De dataset bevat circa 26.400 gesloten en 31.000 open vestigingen, wat overeenkomt met respectievelijk 46,1\% gesloten en 53,9,\% open vestigingen. Dit maakt de dataset relatief gunstig voor een binair classificatieprobleem, aangezien er geen sprake is van een ernstige klasse-onevenwichtigheid die de modelprestaties zou vertekenen.

\begin{figure}[h]
    \centering
    \includegraphics[width=\textwidth]{../proof_of_concept/Grafieken/verdeling_status.png}
    \caption[Verdeling: open vs.\ gesloten]{\label{fig:verdeling open/gesloten}Verdeling van de open en geslotenstatus voor alle merken in de dataset.}
\end{figure}

\subsection{Ontbrekende waarden}
De analyse van ontbrekende waarden onthulde een aantal kolommen die volledig of grotendeels leeg waren in de dataset. Kolommen met 100\% ontbrekende waarden worden automatisch verwijderd uit de dataset alvorens verdere verwerking plaatsvond, 
aangezien deze geen bruikbare informatie kunnen bijdragen aan het model. In de volledige dataset was dit het geval voor \texttt{district\_id}.

Andere kolommen vertoonden een aanzienlijk percentage ontbrekende waarden. Zo ontbrak \texttt{status\_end} in 49,55\% en \texttt{status\_start} in 42,68\% van de records. Dit is te verklaren doordat een ontbrekend eindtijdstip (\texttt{status\_end = Null}) doorgaans aangeeft dat de POI op dat moment nog steeds operationeel is. De openingsuren (\texttt{start.oh} en \texttt{stop.oh}) waren aanwezig in slechts 84,70\% respectievelijk 84,71\% van de records, wat erop wijst dat voor een deel van de POI's geen gestructureerde openingsuren beschikbaar zijn in de databank. Ontbrekende numerieke waarden in de overige feature-kolommen werden aangevuld met de mediaan van de betreffende kolom alvorens het model te trainen.

\begin{listing}[H]
    \begin{minted}{python}
        missing = df_raw.isnull().sum()
        missing_pct = (missing / len(df_raw) * 100).round(2)
        missing_df = pd.DataFrame({"missing": missing, "percentage": missing_pct})
        missing_df[missing_df["missing"] > 0].sort_values("percentage", ascending=False)
    \end{minted}
    \caption[Analyse ontbrekende waarden]{Berekening van het percentage ontbrekende waarden per kolom.}
\end{listing}

\subsection{Verdeling top 15 merken}
Om een beter inzicht te krijgen in de verdeling van de operationele status per merk werden de top 15 meest voorkomende merken gevisualiseerd op basis van hun open en geslotenstatus.

Uit de analyse blijkt dat er een aanzienlijke variatie bestaat in de verhouding tussen open en gesloten vestigingen per merk. Aldi is het meest voorkomende merk in de dataset en vertoont een opvallend hoog aantal gesloten registraties ten opzichte van open, wat kan wijzen op een \textit{decaying}-fase binnen de POI-levenscyclus zoals besproken in sectie~\ref{sec:Levenscyclus} . Hetzelfde patroon is zichtbaar bij Intermarché, Spar, Vival en Super U. Aan de andere kant zijn merken zoals E.Leclerc, Carrefour market en Carrefour express bijna uitsluitend vertegenwoordigd met open vestigingen, wat duidt op een stabiele operationele toestand. Hieruit kunnen we concluderen dat we bij de interpretatie van de modelresultaten rekening moeten houden met een mogelijke geospatiale bias: merken die sterk vertegenwoordigd zijn in de dataset zullen een grotere invloed uitoefenen op de geleerde patronen dan merken met weinig records.

\begin{figure}[h]
    \centering
    \includegraphics[width=\textwidth]{../proof_of_concept/Grafieken/top_brands.png}
    \caption[Top 15 merken: open vs.\ gesloten]{\label{fig:top15merken}Verdeling van de open en geslotenstatus voor de 15 meest voorkomende merken in de dataset.}
\end{figure}
    
\subsection{Verdeling per land}

Om de geografische spreiding van de dataset in kaart te brengen, werd het aantal POI-records per land gevisualiseerd samen met de sluitingsratio per land.

Frankrijk is veruit het best vertegenwoordigde land in de dataset met circa 42.000 POI-records, gevolgd door Nederland met circa 8.000 records en België met circa 7.000 records. Luxemburg is met minder dan 500 records slechts beperkt vertegenwoordigd. Deze onevenwichtige geografische verdeling is een potentiële bron van geospatiale bias. Het model zal voornamelijk patronen leren op basis van Franse supermarkten, wat de generaliseerbaarheid naar kleinere landen zoals Luxemburg kan beperken.

De sluitingsratio verschilt eveneens per land. Luxemburg kent de hoogste sluitingsratio met circa 53\%, gevolgd door Frankrijk met circa 49\%, België met circa 44\% en Nederland met circa 38\%. Deze variatie suggereert dat landspecifieke factoren, zoals marktdynamiek en de mate van dataveroudering per regio, een rol spelen in de statusverdeling. Dit sluit aan bij de bevindingen uit sectie~\ref{sec:Afhankelijkheid}, waarbij \textcite{Boskovic2026} aantonen dat de socio-economische context van een regio samenhangt met de operationele stabiliteit van commerciële vestigingen.

\begin{figure}[h]
      \centering
      \includegraphics[width=\textwidth]{../proof_of_concept/Grafieken/land_status.png}
      \caption[Verdeling per land]{\label{fig:verdelingland}Aantal POI's per land (links) en sluitingsratio per land (rechts).}
\end{figure}

\section{Feature Engineering}
Op basis van de ruwe dataset werden zowel temporele als gedragsmatige kenmerken geconstrueerd die relevant zijn voor de statusdetectie. De features werden afgeleid uit de beschikbare timestamps, openingstijden en historische statusregistraties. Tabel~\ref{tab:features} geeft een overzicht van alle gebruikte features en de betekenis.

\begin{table}[H]
    \centering
    \small
    \begin{tabular}{lll}
        \toprule
        \textbf{Feature} & \textbf{Type} & \textbf{Beschrijving} \\
        \midrule
        \texttt{day\_of\_week} & Temporeel & Dag van de week (1 = maandag, 7 = zondag) \\
        \texttt{is\_weekend} & Temporeel & Binaire vlag: 1 indien zaterdag of zondag \\
        \texttt{heeft\_openingsuren} & Openingsuren & Binaire vlag: 1 indien start- én sluitingsuur beschikbaar zijn \\
        \texttt{start\_min} & Openingsuren & Openingsuur omgezet naar minuten (NaN indien niet beschikbaar) \\
        \texttt{stop\_min}  & Openingsuren & Sluitingsuur omgezet naar minuten (NaN indien niet beschikbaar) \\
        \texttt{verwachte\_openingsduur\_min} & Openingsuren & Verwacht aantal minuten dat de winkel open is \\
        \texttt{closed} & Openingsuren & Binaire vlag uit de openingsuren-tabel \\
        \texttt{ok} & Kwaliteitsvlag & Kwaliteitsvlag van het openingsurenrecord \\
        \texttt{is\_deleted} & POI-status & Binaire vlag: 1 indien POI verwijderd is \\
        \texttt{is\_sensitive} & POI-metadata & Binaire vlag voor gevoelige locaties \\
        \texttt{is\_reserved\_poi} & POI-metadata & Binaire vlag voor gereserveerde POI's \\
        \texttt{cluster} & POI-metadata & Clusteridentificatie van de POI \\
        \texttt{brand\_id\_enc} & POI-metadata & Label-encoded merknummer \\
        \texttt{country\_enc} & POI-metadata & Label-encoded landcode \\
        \texttt{visit} & Gebruikstype & Numerieke classificatie van het bezoektype \\ 
        \texttt{work} & Gebruikstype & Numerieke classificatie van het werktype \\
        \texttt{poi\_leeftijd\_maanden} & Historisch gedrag & Leeftijd van de POI in maanden sinds eerste record \\
        \texttt{n\_observaties\_poi} & Historisch gedrag & Totaal aantal geregistreerde observaties per POI \\
        \bottomrule
    \end{tabular}
    \caption[Overzicht van gebruikte features]{%
        \label{tab:features}Overzicht van de gebruikte features om het model te trainen.}
\end{table}

Alvorens de features te construeren, werden de datumkolommen en numerieke kolommen omgezet naar het juiste datatype.

\begin{listing}[H]
    \begin{minted}{python}
        # Datumkolommen parsen
        for col in ["status_start", "status_end", "inserted_at.poi",
        "updated_at_history", "start_valid", "end_valid"]:
        df[col] = pd.to_datetime(df[col], errors="coerce")
        
        # Numerieke kolommen saneren
        numerieke_cols = [
        "day_of_week", "visit", "work", "closed", "ok",
        "is_deleted", "is_sensitive", "is_reserved_poi", "cluster",
        "brand_id", "priority"
        ]
        for col in numerieke_cols:
        if col in df.columns:
        df[col] = pd.to_numeric(df[col], errors="coerce")
    \end{minted}
    \caption[Datatype conversie]{Omzetting van datumkolommen naar het datetime-formaat en numerieke kolommen naar het juiste datatype.}
\end{listing}

\begin{listing}[H]
    \begin{minted}{python}
        df["is_weekend"] = (df["day_of_week"] >= 5).astype(int)
        
        df["heeft_openingsuren"] = (df["start.oh"].notna() & df["stop.oh"].notna()).astype(int)
        df["start_min"] = df["start.oh"].apply(lambda x: tijd_naar_minuten(x) if pd.notna(x) else np.nan)
        df["stop_min"]  = df["stop.oh"].apply(lambda x: tijd_naar_minuten(x) if pd.notna(x) else np.nan)
        df["verwachte_openingsduur_min"] = df["stop_min"] - df["start_min"]
        
        df["is_deleted"] = df["is_deleted"].fillna(0).astype(int)
        
        le = LabelEncoder()
        df["brand_id_enc"] = le.fit_transform(df["name"].astype(str))
        df["country_enc"]  = le.fit_transform(df["country"].astype(str))
    \end{minted}
    \caption[Feature engineering: overige kenmerken]{Constructie van binaire, openingsuren- en categorische features.}
\end{listing}

\begin{listing}[H]
    \begin{minted}{python}
        df = df.sort_values(["poi_id", "updated_at_history"])
        df["poi_leeftijd_maanden"] = (
        (df["updated_at_history"] - df.groupby("poi_id")["updated_at_history"].transform("min"))
        .dt.days / 30).round()
        df["n_observaties_poi"] = df.groupby("poi_id")["poi_id"].transform("count")
    \end{minted}
    \caption[Feature engineering: temporele kenmerken]{Constructie van de temporele en gedragsmatige features op basis van de historische dataset.}
\end{listing}

Drie van de features verdienen bijzondere aandacht vanwege hun relevantie voor POI-statusdetectie. Ten eerste gaf \texttt{n\_observaties\_poi} het totaal aantal geregistreerde observaties per POI weer. Dit gaf een indicatie van de datadichtheid per vestiging. Ten tweede weerspiegelde \texttt{poi\_leeftijd\_maanden} de periode tussen het eerste geregistreerde record van een POI in de databank en het huidige observatiemoment. Deze feature was relevant voor het model omdat jonge POI's, die nog geen stabiel bezoekpatroon hadden opgebouwd, gevoeliger zijn voor het cold-start probleem, zoals besproken in de literatuurstudie. Ten derde was \texttt{brand\_id\_enc} een label-encoded versie van de naam van het merk. Dit liet toe om merkspecifieke patronen te capteren.

Daarnaast werden ook enkele andere features aangemaakt. Zo werd een feature \texttt{heeft\_openingsuren} gemaakt die aangaf of voor een gegeven observatie zowel een start- als een sluitingsuur aanwezig is. Wanneer openingsuren ontbraken, werd dit geïnterpreteerd als een duidelijk signaal. Een POI zonder geregistreerde openingsuren in de databank kan aangeven dat een point of interest gesloten is, aangezien actieve vestigingen doorgaans  een start- en einduur hebben. De numerieke features \texttt{start\_min} en \texttt{stop\_min} worden bij ontbrekende waarden niet ingevuld met een defaultwaarde. In plaats daarvan werden ze als \texttt{NaN} doorgegeven aan het model.

De twee categorische kolommen \texttt{brand\_id} en \texttt{country} werden omgezet naar numerieke waarden aan de hand van een Label Encoding voor ze als feature aan het model werden aangeboden. Hoewel Label Encoding een ordinale relatie voorstelt die niet noodzakelijk aanwezig is tussen merken of landen, werd voor deze aanpak gekozen omdat dit beter schaalbaar is en beter aansloot bij de gebruikte modelarchitecturen.

\subsection{Train- en testopsplitsing}
De dataset werd opgedeeld in een trainingsset (80\%) en een testset (20\%) via een gelaagde splitsing met een vaste willekeurige seed van 42. Oorspronkelijk werd een temporele splitsing toegepast op basis van de kolom \texttt{updated\_at\_history}, waarbij alle observaties voor de 80\% drempelwaarde aan de trainingsset werden toegewezen. Deze aanpak zorgde echter voor een bias in de klasseverdeling: de testset bevatte 88\% open POI's tegenover 72\% in de trainingsset. Hierdoor was de modelevaluatie onbetrouwbaar. Daarom werd er gekozen om een gelaagde splitsing te gebruiken dat de klasseverdeling van 74,67\% open en 25,33\% gesloten bewaart in zowel de trainingsset als de testset. Het resultaat hiervan is 1.069.823 trainingsrecords en 267.456 testrecords.

\begin{listing}[H]
    \begin{minted}{python}
        df_model = df[FEATURE_COLS + [TARGET_COL]].dropna(subset=[TARGET_COL])
        df_model[FEATURE_COLS] = df_model[FEATURE_COLS].fillna(
        df_model[FEATURE_COLS].median(numeric_only=True)
        )
        
        X = df_model[FEATURE_COLS]
        y = df_model[TARGET_COL].astype(int)
        
        X_train, X_test, y_train, y_test = train_test_split(
        X, y,
        test_size=0.2,
        random_state=RANDOM_SEED,
        stratify=y
        )
        
        print(f"Train: {len(X_train):,} | Test: {len(X_test):,}")
        print(f"Label-verdeling train — open: {y_train.mean():.2%}")
        print(f"Label-verdeling test  — open: {y_test.mean():.2%}")
    \end{minted}
    \caption[Train- en testopsplitsing]{Gelaagde opsplitsing van de dataset in een trainingsset van 80\% en een testset van 20\%.}
\end{listing}

\section{Modellen}
Voor de ontwikkeling van de Proof of concept werden drie machine learning modellen geïmplementeerd en met elkaar vergeleken: Logistische Regressie, Random Forest en XGBoost. Deze keuze is gebaseerd op de classificatiebenaderingen dat in de literatuurstudie aan bod kwamen. Door te kiezen voor zowel een eenvoudig lineair model als complexere boosting modellen kan worden nagegaan welke aanpak het meest geschikt is voor het statusdetectieprobleem.

\subsection{Model 1: Logistische Regressie}
Logistische Regressie werd opgenomen als basismodel om een referentiepunt te bieden voor de complexere modellen. Het model leert een lineaire beslissingsgrens in de feature ruimte en is daardoor eenvoudig te interpreteren: 
de absolute waarde van een coëfficiënt geeft rechtstreeks aan in welke mate een feature bijdraagt aan de voorspelling. Het nadeel hiervan is dat het model niet in staat is om complexe, niet-lineaire verbanden in de data te herkennen, wat bij een probleem zoals POI-statusdetectie een belangrijke beperking kan zijn. Het model maakte gebruik van de standaardparameters van de scikit-learn implementatie.

\begin{listing}[H]
    \begin{minted}{python}
        lr = LogisticRegression(
        class_weight="balanced",
        max_iter=5000,
        random_state=RANDOM_SEED
        )
        lr.fit(X_train, y_train)
    \end{minted}
    \caption[Training Logistische Regressie]{Initialisatie en training van het Logistische Regressie model.}
\end{listing}

\subsection{Model 2: Random Forest}
Random Forest is een ensemblemethode dat een groot aantal beslissingsbomen traint op willekeurige deelsets van de data en features. De uiteindelijke voorspelling kwam tot stand aan de hand van een meerderheidsstemming over alle bomen. Dit maakte het model robuust tegen ruis en minder gevoelig voor overfitting dan een enkelvoudige beslissingsboom. Daarnaast berekent Random Forest automatisch een feature importance score op basis van de gemiddelde afname in onzuiverheden, wat toelaat om de meest relevante kenmerken voor de statusdetectie te identificeren. Het model werd getraind met de standaardparameters van de scikit-learn implementatie.

\begin{listing}[H]
    \begin{minted}{python}
        rf = RandomForestClassifier(
        n_estimators=200,
        max_depth=None,
        min_samples_leaf=5,
        class_weight="balanced",
        random_state=RANDOM_SEED,
        n_jobs=-1
        )
        rf.fit(X_train, y_train)
    \end{minted}
    \caption[Training Random Forest]{Initialisatie en training van het Random Forest model.}
\end{listing}

\subsection{Model 3: XGBoost}
Extreme Gradient Boosting (XGBoost) is een gradiënt-boostingalgoritme dat iteratief zwakke leerders toevoegt, waarbij elke nieuwe boom zich richt op de fouten van de vorige iteratie. Het algoritme staat bekend om zijn sterke 
prestaties op gestructureerde tabeldata en maakte gebruik van de hyperparameters weergegeven in Tabel~\ref{tab:xgb-params} Omdat de dataset een onevenwichtige klasseverdeling vertoonde, werd de \texttt{scale\_pos\_weight} parameter dynamisch ingesteld op de verhouding tussen het aantal negatieve en positieve instanties in de trainingsset, zodat het model niet systematisch de meerderheidsklasse bevoordeelt. Het model maakte gebruik van volgende parameters:

\begin{table}[H]
    \centering
    \small
    \begin{tabular}{ll}
        \toprule
        \textbf{Hyperparameter} & \textbf{Waarde} \\
        \midrule
        \texttt{n\_estimators}      & 300 \\
        \texttt{max\_depth}         & 6 \\
        \texttt{learning\_rate}     & 0,05 \\
        \texttt{subsample}          & 0,8 \\
        \texttt{colsample\_bytree}  & 0,8 \\
        \texttt{scale\_pos\_weight} & verhouding negatief/positief \\
        \texttt{eval\_metric}       & logloss \\
        \texttt{random\_state}      & 42 \\
        \texttt{n\_jobs}            & -1 \\
        \bottomrule
    \end{tabular}
    \caption[XGBoost hyperparameters]{%
        \label{tab:xgb-params}Overzicht van de gebruikte hyperparameters voor het XGBoost-model.}
\end{table}

\begin{listing}[H]
    \begin{minted}{python}
        neg, pos    = (y_train == 0).sum(), (y_train == 1).sum()
        scale_pw    = neg / pos if pos > 0 else 1
        
        xgb_model = xgb.XGBClassifier(
        n_estimators=300,
        max_depth=6,
        learning_rate=0.05,
        subsample=0.8,
        colsample_bytree=0.8,
        scale_pos_weight=scale_pw,
        eval_metric="logloss",
        random_state=RANDOM_SEED,
        n_jobs=-1
        )
        xgb_model.fit(X_train, y_train, eval_set=[(X_test, y_test)], verbose=False)
    \end{minted}
    \caption[Training XGBoost]{Initialisatie en training van het XGBoost model met dynamisch berekende klassengewichten.}
\end{listing}

\section{Evaluatie}

\subsection{Evaluatiemetrieken}
De modellen werden geëvalueerd aan de hand van vijf evaluatiemetrieken: accuracy, precision, recall, F1-score en ROC-AUC. De ROC-AUC score geeft aan hoe goed het model de twee klassen van elkaar kan onderscheiden over alle mogelijke drempelwaarden. Dit is een nuttige aanvulling op de F1-score wanneer de klasseverdeling niet perfect gebalanceerd is.

In de context van POI-statusdetectie is de keuze van de juiste metriek belangrijk. Een hoge precision voor de gesloten klasse betekent dat een POI die als gesloten wordt voorspeld, ook effectief gesloten is. Hierdoor kunnen foutieve updates in de databank worden vermeden. Een hoge recall betekent dat het model zo weinig mogelijk gesloten Point of interests mist. De F1-score combineert beide als het “harmonisch gemiddelde” en geeft dus een evenwichtig beeld.

\subsection{Resultaten}
De resultaten van de definitieve evaluatie op de volledige dataset worden weergegeven in Tabel~\ref{tab:resultaten}.

\begin{table}[H]
    \centering
    \small
    \begin{tabular}{lccccc}
        \toprule
        \textbf{Model} & \textbf{Accuracy} & \textbf{Precision} & \textbf{Recall} & \textbf{F1-score} & \textbf{ROC-AUC} \\
        \midrule
        \multicolumn{6}{l}{\textit{Baseline — historische metadata}} \\
        \midrule
        Logistische Regressie  & 0.66 & 0.81 & 0.71 & 0.76 & 0.65 \\
        Random Forest          & 0.78 & 0.94 & 0.76 & 0.84 & 0.86 \\
        XGBoost                & 0.76 & 0.92 & 0.74 & 0.82 & 0.86 \\
        \midrule
        \multicolumn{6}{l}{\textit{Uitbreiding — kunstmatig gegenereerde mobiliteitsdata}} \\
        \midrule
        Logistische Regressie (mob.) & 0.9992 & 0.9997 & 0.9989 & 0.9993 & 1.00 \\
        Random Forest (mob.)         & 0.9989 & 0.9990 & 0.9990 & 0.9990 & 1.00 \\
        XGBoost (mob.)               & 0.9996 & 1.00   & 0.9993 & 0.9997 & 1.00 \\
        \bottomrule
    \end{tabular}
    \caption[Vergelijking van modelresultaten]{%
        \label{tab:resultaten}Vergelijking van alle modellen op de testset. De bovenste drie rijen betreffen de baseline modellen. De onderste drie betreffen de uitbreidingsmodellen met kunstmatig gegenereerde mobiliteitsfeatures.}
\end{table}

Figuur~\ref{fig:eindvergelijking} geeft een visueel overzicht van alle modellen en hun prestaties op de vijf evaluatiemetrieken.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{../proof_of_concept/Grafieken/eindvergelijking.png}
    \caption[Eindvergelijking alle modellen]{\label{fig:eindvergelijking}
        Vergelijking van alle zes modellen op accuracy, precision, recall, 
        F1-score en ROC-AUC.}
\end{figure}

\subsubsection{Bespreking baseline modellen}
Het Random Forest model behaalde de hoogste F1-score (0.84) en ROC-AUC (0.86). Het XGBoost model behaalde een F1 score van 0.82 en een AUC van 0.86. Logistische Regressie presteert veel slechter met een F1 score van 0.76 en een AUC van 0.65. Dit bevestigt dat de relatie tussen de features en de doelvariabele niet lineair is.

\begin{table}[H]
    \centering
    \small
    \begin{tabular}{lcccc}
        \toprule
        \textbf{Model} & \multicolumn{2}{c}{\textbf{Gesloten}} & 
        \multicolumn{2}{c}{\textbf{Open}} \\
        \cmidrule(lr){2-3} \cmidrule(lr){4-5}
        & \textbf{Precision} & \textbf{F1} & \textbf{Precision} & \textbf{F1} \\
        \midrule
        Random Forest         & 0.54 & 0.67 & 0.94 & 0.84 \\
        XGBoost               & 0.52 & 0.63 & 0.92 & 0.82 \\
        Logistische Regressie & 0.38 & 0.43 & 0.81 & 0.76 \\
        \bottomrule
    \end{tabular}
    \caption[Per-klasse resultaten baseline modellen]{%
        \label{tab:perklasse}Per-klasse precision en F1-score voor de baseline modellen op de testset.}
\end{table}

De gesloten klasse behaalde een F1-score van 0.67 bij Random Forest en 0.63 bij XGBoost. Dit suggereert dat statische metadata alleen onvoldoende is om gesloten POI's betrouwbaar te detecteren. Deze vaststelling vormt meteen de motivatie voor de uitbreiding met mobiliteitsdata.

Figuur~\ref{fig:cm_rf}, \ref{fig:cm_xgb} en \ref{fig:cm_lr} tonen de confusion matrices voor de drie baseline modellen.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{../proof_of_concept/Grafieken/conv_matrix_random_forest.png}
    \caption[Confusion matrix Random Forest]{\label{fig:cm_rf}Confusion matrix van het Random Forest model op de testset.}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{../proof_of_concept/Grafieken/conv_matrix_xgboost.png}
    \caption[Confusion matrix XGBoost]{\label{fig:cm_xgb}Confusion matrix van het XGBoost model op de testset.}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{../proof_of_concept/Grafieken/conv_matrix_logistic_regression.png}
    \caption[Confusion matrix Logistische Regressie]{\label{fig:cm_lr}Confusion matrix van het Logistische Regressie model op de testset.}
\end{figure}

\subsubsection{Bespreking mobiliteitsmodellen}
De mobiliteitsmodellen behaalden opvallend hoge scores. Dit is een rechtstreeks gevolg van de kunstmatig gegenereerde aard van de data. Omdat de bezoekersprofielen gegenereerd worden op basis van expliciete faseregels, zijn de klassen nagenoeg perfect scheidbaar. De feature importance analyse toont aan dat \texttt{gem\_bezoekers} (0.247) en \texttt{std\_bezoekers} (0.178) de sterkste voorspellers zijn. Dit bevestigt dat de dynamische bezoekerspatronen theoretisch een sterk signaal vormen voor statusdetectie, zoals besproken in sectie~\ref{sec:gegevensbronnen}. Deze resultaten geven dus eerder een indicatie van wat mogelijk zou zijn met echte mobiliteitsdata.

\subsection{Feature-importance}

Een analyse van de feature importance laat toe om te begrijpen welke kenmerken het sterkst bijdragen aan het model. Voor het Random Forest model werd de Gini-importance gebruikt. Dit geeft weer hoeveel de gemiddelde onzuiverheid van de knooppunten afneemt wanneer een bepaalde feature wordt gebruikt voor de splitsing. Voor het XGBoost-model werd de ingebouwde feature importance score berekend op basis van het gemiddeld aantal keren dat een feature wordt gebruikt in de “boosting” rondes. Voor de Logistische Regressie gold de absolute waarde van de modelcoëfficiënt als norm.

Figuur~\ref{fig:fi_lr} geeft de feature importance voor het Logistische Regressie model weer. Wat meteen opvalt is dat de openingsuren features hier een belangrijkere rol spelen dan bij de andere twee modellen. Dit is waarschijnlijk te verklaren doordat Logistische Regressie enkel lineaire verbanden kan leggen en de aanwezigheid van openingsuren samenhangt met het feit of een POI open is. Het model presteert relatief zwak ten opzichte van de andere modellen. Dit geeft aan dat deze lineaire verbanden niet volstaan om het volledige probleem te vatten.

\begin{figure}[H]
         \centering
         \includegraphics[width=0.9\textwidth]{../proof_of_concept/Grafieken/feature_importance_lr.png}
         \caption[Feature importance Logistische Regressie]{\label{fig:fi_lr}Top 15 features van het Logistisch Regressie model.}
\end{figure}

Figuur~\ref{fig:fi_rf} toont de top 15 features op basis van de Gini importance voor het Random Forest model. De feature \texttt{n\_observaties\_poi} scoort veruit het hoogst met een importance van 0.30. Dit geeft aan dat het aantal keren dat een POI werd geobserveerd een sterk signaal is voor de operationele status van de POI. Daarnaast blijkt het merk \texttt{brand\_id\_enc} en het land \texttt{country\_enc} ook belangrijk te zijn. Dit is logisch, want uit de data-analyse bleek al dat de sluitingsratio sterk verschilt per merk en per land.
    
\begin{figure}[H]
         \centering
         \includegraphics[width=0.9\textwidth]{../proof_of_concept/Grafieken/feature_importance_random_forest.png}
         \caption[Feature importance Random Forest]{\label{fig:fi_rf}Top 15 features van het Random Forest model.}\\
\end{figure}

Figuur~\ref{fig:fi_xgb} geeft de top 15 features weer voor het XGBoost model. De feature importance lijkt sterk op dat van het Random Forest model: \texttt{n\_observaties\_poi}, \texttt{brand\_id\_enc} en \texttt{country\_enc} zijn opnieuw de belangrijkste features. Aangezien deze twee modellen leiden tot dezelfde conclusies, kunnen we concluderen dat deze kenmerken effectief relevante informatie bevatten voor de statusdetectie.

\begin{figure}[H]
         \centering
         \includegraphics[width=0.9\textwidth]{../proof_of_concept/Grafieken/feature_importance_xgboost.png}
         \caption[Feature importance XGBoost]{\label{fig:fi_xgb}Top 15 features van het XGBoost model.}
\end{figure}

\section{Uitbreiding: kunstmatig gegenereerde mobiliteitsdata}
\label{sec:mobiliteitsdata}

\subsection{Motivatie}
De drie voorgaande modellen baseerden zich uitsluitend op statische metadata. Zoals besproken in de literatuurstudie vormen dynamische mobiliteitspatronen een sterk aanvullend signaal voor de statusdetectie van een POI. Om de potentiële meerwaarde hiervan te onderzoeken, werd een uitbreidingsexperiment opgezet op basis van de basisinfo-dataset (zelfde dataset die gebruikt wordt voor de data analyse).

\subsection{Datageneratie}
Omdat echte GPS-mobiliteitsdata niet beschikbaar was binnen de scope van dit onderzoek, werd deze data per POI gesimuleerd. 

\begin{listing}[H]
    \begin{minted}{python}
        df_mob_basis = df_basis.copy()
        
        # Categorische kolommen omzetten
        le = LabelEncoder()
        for col in ["name", "country", "source", "priority"]:
        if col in df_mob_basis.columns:
        df_mob_basis[f"{col}_enc"] = le.fit_transform(
        df_mob_basis[col].fillna("onbekend").astype(str)
        )
        
        # Numerieke kolommen omzetten
        for col in ["brand_id", "main_category_id", "cluster",
        "is_deleted", "is_sensitive", "is_reserved_poi", "visit", "work"]:
        if col in df_mob_basis.columns:
        df_mob_basis[col] = pd.to_numeric(df_mob_basis[col], errors="coerce")
        
        if "polygon" in df_mob_basis.columns:
        df_mob_basis["heeft_polygon"] = df_mob_basis["polygon"].notna().astype(int)
        if "source_id" in df_mob_basis.columns:
        df_mob_basis["heeft_source_id"] = df_mob_basis["source_id"].notna().astype(int)
        
        for col in ["inserted_at", "updated_at"]:
        if col in df_mob_basis.columns:
        df_mob_basis[col] = pd.to_datetime(df_mob_basis[col], errors="coerce")
        
        if "inserted_at" in df_mob_basis.columns:
        df_mob_basis["poi_leeftijd_dagen"] = (
        df_mob_basis["inserted_at"].max() - df_mob_basis["inserted_at"]
        ).dt.days
        
        if "updated_at" in df_mob_basis.columns:
        df_mob_basis["dagen_sinds_update"] = (
        df_mob_basis["updated_at"].max() - df_mob_basis["updated_at"]
        ).dt.days
    \end{minted}
    \caption[Voorbereiding Basisinfo-dataset]{Encoding en feature constructie op de momentopname-dataset als voorbereiding op het mobiliteitsexperiment.}
\end{listing}

De simulatie onderscheidt drie fasen die aansluiten bij de levenscyclus van een POI:

\begin{itemize}
    \item Stabiele fase: het bezoekersaantal volgt een realistisch weekpatroon met stochastische ruis ($\sigma$ = 40\% van $\mu$). Met een piek op vrijdag, zaterdag en een dal op zondag.
    \item Decay fase: het bezoekersaantal daalt lineair over tien weken voor de sluiting, met een verhoogde ruis ($\sigma$ = 50\%) om onzekerheid te simuleren.
    \item Gesloten fase: het bezoekersaantal is nagenoeg nul, met een gemiddelde van circa twee bezoekers per dag als gevolg van occasioneel bezoek.
\end{itemize}

\begin{listing}[H]
    \begin{minted}{python}
        WEEK_PATROON    = np.array([0.75, 0.80, 0.82, 0.85, 1.00, 0.95, 0.45])
        BASIS_BEZOEKERS = 250
        DECAY_WEKEN     = 10
        N_WEKEN         = 26  # half jaar
        
        mob_rng = np.random.default_rng(RANDOM_SEED)
        
        def genereer_bezoekersprofiel(poi_id, is_actief, n_weken=N_WEKEN,
        decay_weken=DECAY_WEKEN, basis=BASIS_BEZOEKERS):
        records    = []
        poi_factor = mob_rng.uniform(0.7, 1.3)
        
        for week in range(n_weken):
        for dag in range(7):
        mu_basis = basis * poi_factor * WEEK_PATROON[dag]
        
        if is_actief:
        bezoekers = max(0, int(mob_rng.normal(mu_basis, mu_basis * 0.40)))
        else:
        weken_tot_sluiting = n_weken - week
        if weken_tot_sluiting > decay_weken:
        bezoekers = max(0, int(mob_rng.normal(mu_basis, mu_basis * 0.40)))
        elif weken_tot_sluiting > 2:
        decay_factor = weken_tot_sluiting / decay_weken
        mu_decay     = mu_basis * decay_factor
        bezoekers    = max(0, int(mob_rng.normal(mu_decay, mu_decay * 0.50 + 5)))
        else:
        bezoekers = max(0, int(mob_rng.normal(2, 3)))
        
        records.append({"poi_id": poi_id, "week": week,
            "dag": dag, "bezoekers": bezoekers})
        return pd.DataFrame(records)
    \end{minted}
    \caption[Generatiefunctie synthetische mobiliteitsdata]{Parameters en functie voor het simuleren van dagelijkse bezoekersaantallen per POI over 26 weken.}
\end{listing}

Elk POI-profiel werd gegenereerd over een periode van 26 weken. Om het verschil in bezoekersaantal tussen vestigingen te simuleren, kreeg elk POI een individuele variatiefactor toegepast.

Figuur~\ref{fig:mob_weekprofiel} toont het gesimuleerde bezoekersprofiel per dag van de week voor elk de drie fases.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{../proof_of_concept/Grafieken/mob_weekprofiel_per_fase.png}
    \caption[Synthetisch bezoekersprofiel per fase]{\label{fig:mob_weekprofiel}
        Gemiddeld aantal bezoekers per dag van de week.}
\end{figure}

Figuur~\ref{fig:mob_tijdsverloop} toont het tijdsverloop van het bezoekersaantal voor een voorbeeld van een sluitende supermarkt over 26 weken.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{../proof_of_concept/Grafieken/mob_tijdsverloop.png}
    \caption[Tijdsverloop bezoekersprofiel sluitende POI]{\label{fig:mob_tijdsverloop}
        Tijdsverloop van het bezoekersaantal voor een sluitende supermarkt.}
\end{figure}

\subsection{Mobiliteitsfeatures}
Uit de gegenereerde tijdreeksen werden de volgende features geaggregeerd op POI-niveau:

\begin{itemize}
    \item gem\_bezoekers: gemiddeld dagelijks bezoekersaantal
    \item std\_bezoekers: standaarddeviatie als maat voor variabiliteit
    \item max\_bezoekers: piekbezoekersaantal
    \item pct\_nul\_dagen: aantal dagen met nul bezoekers
    \item trend\_coefficient: geeft aan of het bezoekersaantal stijgt of daalt over de tijd, een negatieve waarde wijst op een dalende trend
\end{itemize}

\begin{listing}[H]
    \begin{minted}{python}
        open_ids     = df_mob_basis[df_mob_basis["is_active"] == 1]["id"].tolist()
        gesloten_ids = df_mob_basis[df_mob_basis["is_active"] == 0]["id"].tolist()
        
        alle_profielen = []
        for poi_id in open_ids:
        alle_profielen.append(genereer_bezoekersprofiel(poi_id, is_actief=True))
        for poi_id in gesloten_ids:
        alle_profielen.append(genereer_bezoekersprofiel(poi_id, is_actief=False))
        
        df_mob_raw = pd.concat(alle_profielen, ignore_index=True)
        
        # Aggregeren op POI-niveau
        agg_per_poi = (
        df_mob_raw.sort_values(["poi_id", "week", "dag"])
        .groupby("poi_id")["bezoekers"]
        .agg(gem_bezoekers="mean", std_bezoekers="std",
        max_bezoekers="max", pct_nul_dagen=lambda x: (x == 0).mean())
        .reset_index()
        )
    \end{minted}
    \caption[Generatie en aggregatie mobiliteitsdata]{Generatie van bezoekersprofielen per POI en aggregatie naar POI-niveau features.}
\end{listing}

De features \texttt{pct\_nul\_dagen} en \texttt{trend\_coefficient} zijn wel berekend maar vielen uiteindelijk buiten de finale feature set. Beide features maakten het voor het model te eenvoudig om de twee klassen te onderscheiden: zo vertoonde \texttt{trend\_coefficient} een gemiddelde waarde van $-$1.02 voor sluitende POI's tegenover $-$0.006 voor open POI's. 

\subsection{Experimentopzet}
De mobiliteitsmodellen maakten gebruik van een combinatie van statische metadata en mobiliteitsfeatures. Vervolgens werden ze vergeleken met de baseline modellen (die enkel op statische metadata gebaseerd zijn). Voor dit experiment werden dezelfde drie modellen gebruikt als bij de baseline: Random Forest, XGBoost en Logistische Regressie. De resultaten zijn weergegeven in Tabel~\ref{tab:resultaten}.

\begin{listing}[H]
    \begin{minted}{python}
        FEATURE_COLS_BASIS = [
        col for col in [
        "name_enc", "country_enc", "source_enc", "priority_enc",
        "brand_id", "main_category_id", "cluster",
        "is_sensitive", "is_reserved_poi",
        "visit", "work", "heeft_polygon", "heeft_source_id",
        "poi_leeftijd_dagen", "dagen_sinds_update",
        ] if col in df_uitgebreid.columns
        ]
        
        MOB_COLS = ["gem_bezoekers", "std_bezoekers", "max_bezoekers"]
        FEATURE_COLS_MOB = FEATURE_COLS_BASIS + MOB_COLS
        
        df_m = df_uitgebreid[FEATURE_COLS_MOB + [TARGET_COL]].dropna(subset=[TARGET_COL])
        df_m[FEATURE_COLS_MOB] = df_m[FEATURE_COLS_MOB].fillna(
        df_m[FEATURE_COLS_MOB].median(numeric_only=True)
        )
        
        X_m = df_m[FEATURE_COLS_MOB]
        y_m = df_m[TARGET_COL].astype(int)
        
        X_train_m, X_test_m, y_train_m, y_test_m = train_test_split(
        X_m, y_m, test_size=TEST_SIZE, random_state=RANDOM_SEED, stratify=y_m
        )
    \end{minted}
    \caption[Train/test-splitsing mobiliteitsmodellen]{Opbouw van de feature set en gelaagde splitsing voor de mobiliteitsmodellen.}
\end{listing}

\section{Beperkingen van de huidige POC}
De huidige implementatie kent een aantal beperkingen die in acht dienen te worden genomen bij de interpretatie van de resultaten.

Ten eerste omvat de dataset vier landen met een sterk ongelijke verdeling: Frankrijk is veruit het meest vertegenwoordigd met circa 42.000 records, terwijl Luxemburg slechts 500 records heeft. Dit betekent dat het model voornamelijk patronen leert op basis van Franse supermarkten. Het is dus mogelijk dat het model minder goed presteert voor kleinere landen, zoals Luxemburg. Dit komt doordat het voor deze soort landen veel minder voorbeelden heeft gezien tijdens het trainen van de modellen.

Ten tweede wordt er gebruik gemaakt van een gelaagde splitsing in plaats van een temporele splitsing. Dit is nodig omdat deze splitsing een ongelijke verdeling geeft tussen de trainings- en testset. Het nadeel hiervan is dat het model mogelijk patronen leert die niet zouden kloppen voor de toekomstige data. Dit zou dus een beperking kunnen zijn, als het model later in de praktijk zou worden ingezet.

Ten derde was er geen echte mobiliteitsdata beschikbaar voor dit onderzoek. Zoals besproken in sectie~\ref{sec:gegevensbronnen} wordt zulke mobiliteitsdata beschouwd als een sterk signaal voor het detecteren van statuswijzigingen. Hoewel er een uitbreidingsexperiment is opgezet met kunstmatig gegenereerde bezoekersdata, blijft dit een benadering van de werkelijkheid. Daarnaast zijn de baseline modellen volledig gebaseerd op statische kenmerken van de POI, zoals het merk, het land en de openingsuren. Dit is dus een beperking ten opzichte van modellen die wel dynamische data gebruiken.

Ten vierde is er geen hyperparametertuning uitgevoerd. Dit was een bewuste keuze omdat het doel van deze Proof of Concept is om aan te tonen dat automatische statusdetectie haalbaar is en niet om het absolute beste model te ontwikkelen. Dit wilt zeggen dat de modellen zijn getraind op parameters die handmatig worden ingesteld. Het is dus mogelijk dat de modellen nog beter zouden presteren als de parameters systematisch worden afgestemd via technieken zoals grid search.

Ten vijfde kan het probleem van data-inflatie, zoals besproken in sectie~\ref{sec:uitdagingen}, ook hier een grote rol spelen. Dit betekent dat wanneer een databron besluit om zijn manier van registreren aan te passen, bestaande Point of interest records plots als nieuw kunnen worden beschouwd. In dat geval zou een supermarkt die al jaren in de databank staat opnieuw als een nieuwe vestiging worden opgenomen in de databank, terwijl er in werkelijkheid niets veranderd is aan de fysieke locatie van de POI. Dit kan ervoor zorgen dat er foute labels in de trainingsdata terechtkomen, waarbij een POI als open wordt beschouwd terwijl deze in werkelijkheid al een tijdje gesloten zou zijn.

Tot slot zijn de resultaten van het mobiliteitsexperiment gebaseerd op kunstmatig gegenereerde bezoekersdata. De bezoekersprofielen zijn gesimuleerd op basis van realistische aannames over het gedrag van de supermarktbezoekers, zoals een hogere drukte op vrijdag en zaterdag. Daarnaast is er een geleidelijke daling in het bezoekersaantal in de weken voor een sluiting. Hoewel deze aannames onderbouwd zijn, blijft het een vereenvoudiging van de werkelijkheid. Zo bevat echte mobiliteitsdata veel meer variatie in de data en ruis. Denk bijvoorbeeld aan tijdelijke sluitingen door verbouwingen, feestdagen of weersomstandigheden. De resultaten van dit experiment geven dan ook enkel een indicatie van wat mogelijk zou zijn met echte mobiliteitsdata. De integratie van echte data is dan ook een interessante piste voor toekomstig onderzoek.

\section{Implementatiedetails}
De volledige POC is geïmplementeerd in Python 3.12.8. Tabel~\ref{tab:bibliotheken} geeft een overzicht van de gebruikte bibliotheken.

\begin{listing}[H]
    \begin{minted}{python}
        DATA_PATH   = "../../data accurat/Data_accurat.csv"
        MODEL_DIR   = "./models"
        RANDOM_SEED = 42
        TEST_SIZE   = 0.2
        
        import numpy as np
        import pandas as pd
        import matplotlib.pyplot as plt
        import seaborn as sns
        from sklearn.model_selection import train_test_split
        from sklearn.ensemble import RandomForestClassifier
        from sklearn.preprocessing import LabelEncoder
        from sklearn.metrics import (
        accuracy_score, precision_score, recall_score,
        f1_score, roc_auc_score
        )
        from sklearn.linear_model import LogisticRegression
        import joblib
        import xgboost as xgb
    \end{minted}
    \caption[Configuratie en bibliotheken]{Configuratie van de notebook en importeren van de gebruikte bibliotheken.}
\end{listing}

\begin{table}[H]
    \centering
    \small
    \begin{tabular}{ll}
        \toprule
        \textbf{Bibliotheek} & \textbf{Gebruik} \\
        \midrule
        \texttt{pandas}      & Inladen en verwerking van de data \\
        \texttt{numpy}       & Numerieke berekeningen \\
        \texttt{scikit-learn} & Logistische Regressie, Random Forest, Label Encoding, \\
        & evaluatiemetrieken en train-testsplitsing \\
        \texttt{xgboost}     & XGBoost model \\
        \texttt{matplotlib}  & Visualisaties \\
        \texttt{seaborn}     & Visualisaties \\
        \texttt{joblib}      & Opslaan van getrainde modellen \\
        \bottomrule
    \end{tabular}
    \caption[Gebruikte bibliotheken]{\label{tab:bibliotheken}Overzicht van de gebruikte Python bibliotheken en hun toepassing binnen de POC.}
\end{table}

De getrainde modellen worden opgeslagen in een lokale map (\texttt{./models}) aan de hand van \texttt{joblib.dump()}. Dit zorgt ervoor dat de modellen later opnieuw kunnen worden gebruikt zonder ze opnieuw te moeten trainen.